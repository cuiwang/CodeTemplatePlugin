# Changelog

## [1.0.1] - 2026-04-24

### Fixed

- 修复 `Settings > Code Template` 中表格名称/内容编辑时频繁自动失去焦点的问题。
- 增强右键菜单图标显示兼容兜底逻辑，提升不同 IDE 环境下图标可见性。
- 修复插件描述校验问题，确保满足 JetBrains Marketplace 的描述规则。

## [1.0.0] - 2026-04-24

### Added

- 初始版本发布。
- 支持编辑器右键菜单 `Code Template`。
- 支持通过 `Create` 从选区创建模板。
- 支持通过 `Insert` 动态菜单插入模板。
- 支持快捷键呼出模板插入弹窗（默认 `Ctrl+I` / macOS `Cmd+I`）。
- 支持数字键、方向键、回车快速插入。
- 支持 `Settings > Other Settings > Code Template` 配置页。
- 支持模板启用状态管理与 JSON 导入导出。
